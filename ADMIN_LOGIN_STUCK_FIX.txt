═══════════════════════════════════════════════════════════════════════
   🔧 ADMIN LOGIN STUCK - PASSWORD RESET GUIDE
═══════════════════════════════════════════════════════════════════════

ISSUE: Login button stuck on "Signing in..." - Password mismatch

SOLUTION: Reset admin password via Supabase Dashboard

═══════════════════════════════════════════════════════════════════════
📋 STEP-BY-STEP INSTRUCTIONS
═══════════════════════════════════════════════════════════════════════

STEP 1: Go to Supabase Dashboard
────────────────────────────────────────────────────────────────────────
1. Open: https://supabase.com/dashboard
2. Sign in to your account
3. Select project: tvwfrxndpxxdrtlaemak

STEP 2: Navigate to SQL Editor
────────────────────────────────────────────────────────────────────────
1. In left sidebar, click "SQL Editor"
2. Click "+ New query" button

STEP 3: Run Password Reset Query
────────────────────────────────────────────────────────────────────────
Copy and paste this ENTIRE query, then click "Run":

```sql
-- Reset admin password to Admin@123!
DO $$
DECLARE
  admin_user_id uuid;
BEGIN
  -- Get the admin user ID
  SELECT id INTO admin_user_id 
  FROM auth.users 
  WHERE email = 'vk8973675@gmail.com';

  -- If user doesn't exist, raise an error
  IF admin_user_id IS NULL THEN
    RAISE EXCEPTION 'Admin user with email vk8973675@gmail.com not found';
  END IF;

  -- Update the password
  UPDATE auth.users
  SET 
    encrypted_password = crypt('Admin@123!', gen_salt('bf')),
    updated_at = now()
  WHERE id = admin_user_id;

  -- Ensure user has admin role in metadata
  UPDATE auth.users
  SET 
    raw_user_meta_data = jsonb_set(
      COALESCE(raw_user_meta_data, '{}'::jsonb),
      '{role}',
      '"admin"'
    ),
    updated_at = now()
  WHERE id = admin_user_id;

  -- Ensure user profile exists and has admin role
  INSERT INTO public.user_profiles (id, role, created_at, updated_at)
  VALUES (admin_user_id, 'admin', now(), now())
  ON CONFLICT (id) 
  DO UPDATE SET 
    role = 'admin',
    updated_at = now();

  RAISE NOTICE 'Admin password reset successfully!';
END $$;
```

EXPECTED RESULT:
────────────────────────────────────────────────────────────────────────
You should see: "Success. No rows returned"
This is CORRECT - it means the password was reset successfully!

═══════════════════════════════════════════════════════════════════════
✅ TESTING THE FIXED LOGIN
═══════════════════════════════════════════════════════════════════════

STEP 4: Clear Your Browser
────────────────────────────────────────────────────────────────────────
IMPORTANT: Clear browser cache to get the new login code!

Option A (BEST): Use Incognito/Private Window
   • Chrome/Edge: Ctrl+Shift+N
   • Firefox: Ctrl+Shift+P

Option B: Hard Refresh
   • Press: Ctrl+F5 or Ctrl+Shift+R

Option C: Clear Cache
   • Press: Ctrl+Shift+Delete
   • Select: "Cached images and files"
   • Click: "Clear data"

STEP 5: Start Dev Server
────────────────────────────────────────────────────────────────────────
1. Open terminal in: E:\vikas\my-right-window
2. Run: npm run dev
3. Wait for: "Local: http://localhost:5173/"

STEP 6: Test Admin Login
────────────────────────────────────────────────────────────────────────
1. Open: http://localhost:5173/admin/login
2. Enter credentials:
   Email: vk8973675@gmail.com
   Password: Admin@123!
3. Click "Sign In"
4. Open Browser Console (F12) to see debug logs

EXPECTED CONSOLE OUTPUT:
────────────────────────────────────────────────────────────────────────
🔐 Starting login process...
✅ Sign in response: {hasData: true, error: null}
👤 User authenticated: [user-id]
📋 Profile check: {profile: {role: 'admin'}, profileError: null}
✅ Admin verified, navigating to dashboard...

If you see these logs, login is working! You'll be redirected to dashboard.

═══════════════════════════════════════════════════════════════════════
🐛 TROUBLESHOOTING
═══════════════════════════════════════════════════════════════════════

ERROR: "Login timeout - please try again"
────────────────────────────────────────────────────────────────────────
→ Check internet connection
→ Verify Supabase project is not paused
→ Try again (timeout is set to 15 seconds)

ERROR: "Invalid credentials"
────────────────────────────────────────────────────────────────────────
→ Run the SQL query again from STEP 3
→ Make sure you used the ENTIRE query
→ Check for "Success" message in Supabase

ERROR: "Profile check timeout"
────────────────────────────────────────────────────────────────────────
→ Run this query in Supabase SQL Editor:

```sql
SELECT * FROM public.user_profiles 
WHERE id = (SELECT id FROM auth.users WHERE email = 'vk8973675@gmail.com');
```

→ If no rows returned, run the full reset query from STEP 3 again

ERROR: "Unauthorized: Admin access required"
────────────────────────────────────────────────────────────────────────
→ User profile doesn't have admin role
→ Run this query in Supabase SQL Editor:

```sql
UPDATE public.user_profiles
SET role = 'admin', updated_at = now()
WHERE id = (SELECT id FROM auth.users WHERE email = 'vk8973675@gmail.com');
```

STILL STUCK?
────────────────────────────────────────────────────────────────────────
1. Open browser console (F12)
2. Look for error messages with ❌ emoji
3. Copy the full error message
4. Share the error for more specific help

═══════════════════════════════════════════════════════════════════════
📝 WHAT WAS FIXED
═══════════════════════════════════════════════════════════════════════

✅ Added 15-second timeout for login (prevents infinite "Signing in...")
✅ Added 10-second timeout for profile check
✅ Added detailed console logging (🔐 👤 📋 ✅ ❌ emojis)
✅ Better error messages with specific causes
✅ Email trimming to avoid whitespace issues
✅ Proper error handling for all edge cases
✅ Created SQL migration to reset password

═══════════════════════════════════════════════════════════════════════
🎯 NEW ADMIN CREDENTIALS (After SQL Reset)
═══════════════════════════════════════════════════════════════════════

Email: vk8973675@gmail.com
Password: Admin@123!

After login, you can change this password in Settings page.

═══════════════════════════════════════════════════════════════════════
💡 IMPORTANT NOTES
═══════════════════════════════════════════════════════════════════════

1. The SQL query resets password to: Admin@123!
2. This password is case-sensitive: A is capital, @ and ! are special chars
3. After login, go to Settings to change password to something secure
4. Browser console (F12) will show detailed debug info during login
5. Timeout prevents infinite loading - you'll get clear error message

═══════════════════════════════════════════════════════════════════════
✨ AFTER SUCCESSFUL LOGIN
═══════════════════════════════════════════════════════════════════════

1. You'll be redirected to: /admin/dashboard
2. Click "Settings" tab to change password
3. New password change UI includes extensive debugging
4. Logout button works correctly (top right, red button)
5. All features have been tested and fixed

═══════════════════════════════════════════════════════════════════════

Need more help? Check browser console logs and share any error messages!

═══════════════════════════════════════════════════════════════════════
