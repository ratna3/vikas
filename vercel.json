{
  "buildCommand": "cd my-right-window && npm install && npm run build",
  "outputDirectory": "my-right-window/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
